apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qos-alert-adaptor.fullname" . }}-test-connection"
  labels:
    {{- include "qos-alert-adaptor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qos-alert-adaptor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
