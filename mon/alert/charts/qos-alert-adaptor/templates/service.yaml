apiVersion: v1
kind: Service
metadata:
  name: {{ include "qos-alert-adaptor.fullname" . }}
  labels:
    {{- include "qos-alert-adaptor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qos-alert-adaptor.selectorLabels" . | nindent 4 }}
